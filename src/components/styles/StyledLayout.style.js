import styled from "styled-components/macro";

const StyledLayout = styled.div`
    display: flex;
    flex-direction: column;
    align-items: center;
    .item {
        color: white;
        padding: 10px;
    }
`;

export { StyledLayout };